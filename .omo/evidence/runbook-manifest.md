# Runbook Manifest (live, Phase 5 flipped)

> **Generated by:** Prometheus-planner · **Status:** `DRAFT — awaiting F3 approval` · **Phase 5:** `live (config-first)`

## target

- **Repo:** `/home/olly/core-engineering-system/`
- **Remote (push NOT authorized):** `https://github.com/Tizzle716/core-engineering-system` on `dev`

## human_in_the_loop_required

`true`

### checkpoints (11 total — 7 prior + 4 Phase-5 extensions)

Prior (verbatim):

1. `git commit` on local `dev` branch
2. `git push origin dev`
3. `terraform plan`
4. `terraform apply`
5. `gcloud builds submit` / `gcloud run deploy`
6. `wrangler deploy` / KVs / queues
7. Infisical Universal Auth handshake

New (Phase 5):

8. `@agency-agents/cli` global install (`-g`); local `agency-agents/node_modules/` install is default
9. MCP system-wide registry edit (e.g. `mcp register agency-agents-factory --system`)
10. Antigravity IDE / Taskmaster.ai live API call
11. `docker compose up agency-agents-daemon` (port-bind 8082)

## credentials_directories_forbidden

- `/home/olly/.gemini`
- `/home/olly/.google-cloud-sdk`
- `/home/olly/Infisical*`
- `/home/olly/.aws`
- `/home/olly/.netrc`
- `*.key`, `*.pem`, `service-account.json`

## system_wide_config_forbidden (NEW — Phase 5)

- `~/.config/mcp/`
- `~/.mcp/`
- `~/.opencode/`
- `~/.claude/`
- `~/.codex/`
- `~/.npmrc` containing `-g` register redirects

## out_of_scope_paths

- `/AISSC_Cloud_Workspace`
- `/CORE-Modules`
- `/Inference-Tokenomics`
- `/EIM-Core-Modules`

## phases

### phase_1_cryptographic_scrape

- **in_scope:** `false`
- **rationale:** Not a goal. No credentials harvest.
- **blocked:** reading `.gemini`, `.google-cloud-sdk`, Infisical; Universal Auth call; `recovery_manifest.json` export.
- **evidence_pointer:** `evidence/PHASE-1.md`

### phase_2_cloudflare_edge

- **in_scope:** `paper-only`
- **rationale:** Design comments in `cloudbuild.yaml`; no live provisioning.
- **planned:** commented placeholders for `OBSERVABILITY_STATE`, `INFISICAL_CACHE`, `remediation-events`.
- **blocked:** `wrangler kv:namespace create`, `wrangler queues create`, `wrangler deploy`, `cloudflared`.
- **evidence_pointer:** `evidence/PHASE-2.md`

### phase_3_cpa_integration

- **in_scope:** `true` (in-repo only)
- **planned:** `.test/`, `track-a/opa_eval.py`, `.test/test_opa_eval.py`.
- **evidence_pointer:** `evidence/PHASE-3.md`

### phase_4_cloud_build

- **in_scope:** `config-only`
- **planned:** path-aware triggers, `--no-allow-unauthenticated`, `cpu=1`, `memory=512Mi`.
- **evidence_pointer:** `evidence/PHASE-4.md`

### phase_5_engine_init (LIVE — flipped this turn)

- **in_scope:** `live`
- **tasks:**
  - 8. Install `@agency-agents/cli` to `agency-agents/node_modules/` (local default; `-g` gated)
  - 9. Author `mcp/tools/agency-agents-factory.json` MCP tool definition + `mcp/README.md` (repo-local)
  - 10. Configure Antimravity IDE + Taskmaster.ai codeboard-generator combo — **config-only** (`live: false` flag in JSON spec; live API gated by checkpoint #10)
  - 11. Stand up `agency-agents/` daemon skeleton (Dockerfile, main.py offline-self-test, teams.json with isolation contract; **port-bind gated by checkpoint #11**)
- **blocked_for_live:**
  - `npm install -g @agency-agents/cli`
  - `mcp register agency-agents-factory --system` or any system-wide MCP registry edit
  - `curl https://api.antigravity.dev/...` / `curl https://api.taskmaster.ai/...`
  - `docker compose up agency-agents-daemon` (port-bind 8082)
- **isolation_contract:**
  - per-team docker compose namespace
  - tmpfs `/var/ledger`
  - non-root user inheritance (`coreengine`)
  - no `allUsers` IAM binding
  - allowlist subset scoped to that team
- **evidence_pointer:** `evidence/PHASE-5.md`

## mcp_tools (new — Phase 5)

- **`agency-agents-factory`**
  - repo-local path: `mcp/tools/agency-agents-factory.json`
  - backends: `antigravity-ide`, `taskmaster-ai`
  - daemon: `agency-agents-daemon:8082`
  - schema-strict: name, version, description, inputs {module_request, team_size}, outputs {team_id, verification_plan}
  - isolation_contract: see phase_5 entry above
  - live-registration: GATED by checkpoint #9

- **`codeboard-combo`** (helper, repo-local)
  - repo-local path: `mcp/tools/codeboard-combo.json` + `mcp/tools/codeboard-combo.dryrun.py`
  - flag `live: false` (config-only by default)
  - live-call: GATED by checkpoint #10

## must_not_have_summary_count

`14` — see plan's `Must NOT Have` table (extended).

## human_in_the_loop_count

`11` — see top of file.
